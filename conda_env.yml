name: svm
channels:
  - defaults
dependencies:
  - numpy
  - scipy
  - scikit-learn
  - matplotlib
  - numba
  - pytest
  - pip
  - pip:
      - cppimport
      - pybind11
      - pylint
prefix: /home/edwinb/anaconda3/envs/svm
